{
  "name": "firebase-monitor-pro",
  "version": "2.0.0",
  "private": true,
  "main": "api/webhook.js",
  "scripts": {
    "start": "node -e \"require('./src/bot')\"",
    "check": "node --check src/security.js && node --check src/database.js && node --check src/monitor.js && node --check src/bot.js && node --check api/webhook.js"
  },
  "dependencies": {
    "firebase-admin": "^12.0.0",
    "telegraf": "^4.15.3"
  }
}
